# Copy this file to .env and adjust values as needed for your environment.
# All variables have safe defaults defined in docker-compose.yml, but
# providing explicit values ensures predictable configuration.

APP_DOMAIN=macromv.com
APP_URL=https://macromv.com
SERVER_PORT=8080
BACKEND_PORT=8080
FRONTEND_PORT=80
OLLAMA_PORT=11434
DB_PORT=3306

# Uncomment and adjust any sensitive credentials required by the
# application (these should remain only in your local .env file):
# DB_ROOT_PASSWORD=change-me
# DB_USER=jm
# DB_PASSWORD=secret
# JWT_SECRET=super-secret
# STRIPE_SECRET_KEY=sk_live_...
