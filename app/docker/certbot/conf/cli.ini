# Default Certbot configuration for the JM stack
# The email is provided via the CERTBOT_EMAIL environment variable in docker-compose.

webroot-path = /var/www/certbot
agree-tos = true
non-interactive = true
preferred-challenges = http
rsa-key-size = 4096
text = true
